const ModalContent = {
  name: "Modal.Content",
  type: "component",
  config: {
    div: {
      className: "modal-content",
      children: [
        {
          div: {
            innerText: "This is modal window dialog"
          }
        }
      ]
    }
  }
};

export default ModalContent;
