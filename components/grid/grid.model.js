const GridModel = {
  name: "Grid",
  type: "model",
  config: {
    sort: async (model, state) => {
      // nothing here
    },
    oninitelement: (model, state) => {
      // nothing here
    },
  },
};

export default GridModel;
